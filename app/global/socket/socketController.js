(function () {
    var app = angular.module("pioneerRoad");

    app.controller("socketController", ['$scope', '$localStorage', '$rootScope', function ($scope, $localStorage, $rootScope, chatSocket) {
            
        }]);
}());